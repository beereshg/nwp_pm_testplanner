# Deep Analysis: Near TDP

| Field | Value |
|-------|-------|
| **HSD ID** | 22022422440 |
| **Title** | Near TDP |
| **Date** | 2025-07-24 |
| **Target Program** | NWP (Newport) |
| **Source Program** | DMR (Diamond Rapids) |
| **Segment** | FV |
| **Feature** | Pstate Stack |
| **Sub-Feature** | NearTDP — PTU voltage increase approaching TDP; B2P_EXCHANGE HPM path |
| **NWP Disposition** | **Runnable_On_N-1** |

---

## Section A: NWP Disposition & Justification

**Disposition: Runnable_On_N-1**

This test verifies the **NearTDP** feature using the PTU (Platform Test Utility) tool:
1. PTU iteratively increases voltage on Core, Fabrics, and other domains until power approaches TDP
2. Initiated via OS mailbox (uCR package-scoped register in TPMI space)
3. Primecode validates config, sends HPM commands via `CBB B2P_EXCHANGE`
4. Root primary propagates to secondary primecode (IMH die fabrics and adjacent CBBs)

On NWP (single iMH, 2 CBBs):
- No secondary primecode for IMH (NWP has single iMH with single PCode instance)
- `B2P_EXCHANGE` HPM path exists per CBB
- TPMI uCR mailbox same mechanism

**Key Justification:**
- NearTDP feature applicable on NWP
- `plc.feature.p2` + `NGA_MAIN` + `PMSS_NWP_READINESS_CHECK` tags
- PTU platform-agnostic tool

---

## Section B: NWP-Specific Test Procedure

### NearTDP Architecture

```
PTU → OS TPMI mailbox (uCR) → Root PCode → B2P_EXCHANGE HPM to each CBB
                                          → NWP: 2 CBBs (no secondary IMH PCode)
```

### Adapted Test Steps

| Step | Action | NWP Details |
|------|--------|-------------|
| 1 | Boot NWP normally | Standard SVOS |
| 2 | Run NearTDP check | `python runPmx.py -x nwp.xml -p neartdp_check -tM 60` |
| 3 | PTU sends voltage increase requests | Iterative steps from PTU |
| 4 | Verify Primecode accepts HPM voltage changes | B2P_EXCHANGE to CBB0, CBB1 |
| 5 | Monitor power approaching TDP | PKG_POWER_INFO MSR or TPMI |
| 6 | Verify PTU PASS: power stayed near TDP without errors | No PROCHOT, no MCA |

### NWP-Specific Differences

| Aspect | DMR | NWP |
|--------|-----|-----|
| IMH instances | 2 (root + secondary) | **1** (single iMH) |
| CBBs | 2–4 | **2** |
| HPM propagation | Root → secondary iMH | Root → 2 CBBs directly |
| TPMI uCR mailbox | Same | Same |

### NWP Pass Criteria
- PTU voltage increase accepted by Primecode
- HPM applied to both CBBs (B2P_EXCHANGE)
- Power approaches TDP within tolerance
- No MCA or stability errors during NearTDP sweep

---

## Section F: Recommendation

**Recommendation: ADOPT — `dmr.xml` → `nwp.xml`; NearTDP HPM path adapted for single iMH**

Required adaptations:
1. `python runPmx.py -x nwp.xml -p neartdp_check -tM 60`
2. NWP: single iMH — no secondary primecode HPM step; verify test script handles this
3. 2 CBBs on NWP; verify both CBBs receive voltage HPM commands

**Priority**: Medium — `NGA_MAIN` + `plc.feature.p2`; NearTDP voltage increase margin test
