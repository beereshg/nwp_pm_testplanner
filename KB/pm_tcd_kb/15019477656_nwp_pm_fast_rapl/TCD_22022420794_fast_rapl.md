# TCD: Fast RAPL

| Field | Value |
|-------|-------|
| **TCD ID** | [22022420794](https://hsdes.intel.com/appstore/article-one/#/22022420794) |
| **Title** | Fast RAPL |
| **Status** | open |
| **Owner** | mps |
| **Parent TP** | [15019477656 — NWP PM Fast RAPL](https://hsdes.intel.com/appstore/article-one/#/15019477656) |
| **Feature** | Power / RAPL — Fast RAPL |
| **Sub-Feature** | Fast RAPL — sub-ms PEM/SVID IMON power excursion response |
| **NWP Disposition** | Runnable_On_N-1 (FV team assessment; see NWP status note) |
| **KB last updated** | 2026-06-25 |

## Section 1: Architecture / Micro-architecture and Functionality

**Fast RAPL** is a **sub-millisecond power limiting mechanism** that complements the standard Socket RAPL PL1/PL2 NN-PID control loop. While Socket RAPL PL1/PL2 respond on ~1 ms loop cadence, Fast RAPL responds to transient power excursions at **~500 µs** using **SVID IMON** telemetry and a dedicated **FastRAPL PID**. The PLR (Performance Limit Reason) bit `FAST_RAPL` in the CBB TPMI reflects Fast RAPL throttling activity.

**⚠️ NWP Status Note:** The NWP PM MAS §3 ZBB list includes FastRAPL as ZBB'd ("SIMPL, FastRAPL, Fine Grained Energy"). However, FV team TC inference for the child TCs (22022421939, 22022421944) classifies them as **Runnable_On_N-1** citing "Fast RAPL (PEM/UFS feature) is Supported on NWP." Confirm against the latest NWP PM MAS before silicon validation. TCD content is written assuming Fast RAPL is runnable pending confirmation.

**Key architectural facts (from DMR RAPL Simplification FAS + Primecode Fast RAPL flow + HPM Message Spec):**
- Fast RAPL is a **FastRAPL PID** evaluated and communicated every **500 µs** — all computations and downstream HPM communications run at this cadence
- It does **not** use an EWMA low-pass filter at the front end (unlike Socket RAPL PL1/PL2)
- When instantaneous socket power (measured via SVID IMON) exceeds the Fast RAPL threshold, Fast RAPL computes a frequency ceiling and drives it downstream
- **Threshold is not user-programmable** through standard register interfaces — default is conceptually `Power_Target ≈ 1.2 × TDP`; post-si override hooks exist for debug only
- The frequency ceiling from Fast RAPL is min'd with all other active RAPL controller outputs → unified **`RAPL_PERF_LIMIT`** (HPM opcode 0x14) sent every 500 µs
- DMR **removed** the older dedicated Fast RAPL HPM message — Fast RAPL is now folded into the unified `RAPL_PERF_LIMIT` message with the `FAST_RAPL` indicator bit
- If SVID telemetry is unavailable, Fast RAPL cannot evaluate power → drives max/no-effective-limit (`0xFF`) rather than real throttling
- Response time (PLR) < 4 ms (vs ~25 ms for PL2 settling)
- `PEM_STATUS[FAST_RAPL]` (bit 9) uses **RW0C** semantics

### Block Diagram

```
  SVID IMON (instantaneous socket power measurement, ~500 µs)
         |
         v
  Fast RAPL PEM Loop (500 µs cadence on NIO / IMH-P root die)
  Compare: actual_power vs. Fast_RAPL_threshold
         |  excursion detected
         v
  PEM Excursion Bit asserted
  Fast RAPL frequency ceiling computed
         |
         v
  min(Fast RAPL ceiling, Socket PL1 ceiling, Socket PL2 ceiling, ...)
  = RAPL_PERF_LIMIT
         |
  HPM 0x14 (root NIO -> CBB0, CBB1)
         |
    +-----------+----------+
    |                      |
    v                      v
  CBB0 PCode           CBB1 PCode
  Enforce freq ceiling  Enforce freq ceiling
  Set PLR FAST_RAPL bit Set PLR FAST_RAPL bit
  in tpmi.plr_mailbox   in tpmi.plr_mailbox
    |                      |
    v                      v
  HPM 0x16 LEAF_PERF_STATUS -> Root
  pem_status.pem_excursion_bit per CBB

  NWP: 2 CBBs only (cbb0 + cbb1)
  Root: sv.socket0.nio.punit.* (not sv.socket0.imh0)
```

### TC Coverage Map

| TC ID | Title | NWP Disposition |
|-------|-------|----------------|
| [22022421939](https://hsdes.intel.com/appstore/article-one/#/22022421939) | Fast RAPL - PEM Verification | Runnable_On_N-1 |
| [22022421944](https://hsdes.intel.com/appstore/article-one/#/22022421944) | Fast RAPL - PLR Verification | Runnable_On_N-1 |
| [22022421949](https://hsdes.intel.com/appstore/article-one/#/22022421949) | Fast RAPL with traffic | TBD |
| [22022421955](https://hsdes.intel.com/appstore/article-one/#/22022421955) | Fast RAPL - HPM message verification | TBD |
| [16030715621](https://hsdes.intel.com/appstore/article-one/#/16030715621) | [PSS] PLR - Fast RAPL | PSS |

---

## Section 2: Interfaces and Protocols

| Interface | Register / Path | Purpose |
|-----------|----------------|---------|
| **`tpmi.pem_status`** | `sv.socket0.cbb{0,1}.base.tpmi.pem_status` | Per-CBB PEM excursion status bit; set when Fast RAPL detects power excursion |
| **`tpmi.plr_mailbox_interface`** | `sv.socket0.cbb{0,1}.base.tpmi.plr_mailbox_interface` | Per-CBB PLR mailbox; `FAST_RAPL` bit set when Fast RAPL is throttle cause |
| **`perf_limit_reasons.fast_rapl`** | `sv.socket0.imh0.ptpcfsms.perf_limit_reasons` | IMH-level Fast RAPL perf-limit reason (bit 9 of `CORE_PERF_LIMIT_REASONS`) |
| **HPM 0x14** | Root NIO → CBB0/CBB1 | `RAPL_PERF_LIMIT` distribution including Fast RAPL ceiling |
| **HPM 0x16** | CBBs → Root | `LEAF_PERF_STATUS` including Fast RAPL throttle accounting |
| SVID IMON | SVID bus (NIO as master) | Instantaneous socket power measurement input to Fast RAPL PEM loop |
| TPMI `socket_rapl_energy_status` | `sv.socket0.imh0.ptpcfsms.socket_rapl_energy_status` | Energy accumulation counter (should increment during Fast RAPL throttle) |

---

## Section 3: Reset, Power, and Clocking

- Fast RAPL PEM loop runs on the **NIO / IMH-P root die** at ~500 µs cadence; initialized during PH6
- Requires SVID bus operational and IMON telemetry available
- Fast RAPL threshold is configured by PrimeCode from TPMI / fuse inputs at boot
- Warm reset re-initializes Fast RAPL state; cold reset resets all RAPL controllers including Fast RAPL
- BIOS knobs `TurboPowerLimitLock` and `TurboPowerLimitCsrLock` must be **disabled** for runtime testing

---

## Section 4: Programming Model

- **Fast RAPL threshold**: **Not user-programmable** through standard register interfaces — configured by PrimeCode from TDP fuse (`Power_Target ≈ 1.2 × TDP` default); post-si debug override hooks exist but are not product interfaces
- **PEM status**: `sv.socket0.cbb{0,1}.base.tpmi.pem_status.pem_excursion_bit` (bit 9) — **RW0C** semantics; clear by writing 0; avoid race conditions in reads
- **PLR mailbox**: `sv.socket0.cbb{0,1}.base.tpmi.plr_mailbox_interface` — `FAST_RAPL` bit set when Fast RAPL is throttle cause
- **`RAPL_PERF_LIMIT` (HPM 0x14)**: Unified message carrying `FAST_RAPL` indicator bit + resolved `RAPL_PID_FREQ_OUTPUT` + `CLOS_*_LIMIT` outputs — sent every 500 µs when Fast RAPL is active
- **SVID unavailable fallback**: If SVID/IMON telemetry is unavailable, Fast RAPL drives `0xFF` (max/no-effective-limit) rather than throttling — verify SVID health before testing
- **Test stimulus**: PMX `cpu_rapl` module with `nwp.xml` → `python runPmx.py -x nwp.xml -p cpu_rapl`; or PTU workload to exceed Fast RAPL threshold
- **NWP CBB count**: Loop over `cbb0`, `cbb1` only (not `range(4)`)
- **Root die path**: `sv.socket0.nio.punit.*` for root-die TPMI (not `imh0`)

---

## Section 5: Operational Behavior

**Fast RAPL entry flow:**
1. NIO PrimeCode 500 µs timer fires; reads SVID IMON for instantaneous socket power
2. FastRAPL PID evaluates: actual power vs. threshold (`≈ 1.2 × TDP`)
3. If power exceeds threshold: Fast RAPL computes frequency ceiling; `RAPL_PERF_LIMIT = min(Fast RAPL, PL1, PL2, ...)` updated
4. **HPM 0x14** (`RAPL_PERF_LIMIT`) distributed to CBB0 and CBB1 with `FAST_RAPL` bit set — **every 500 µs** (decoupled from 1 ms Socket RAPL subflow)
5. Each CBB PCode applies frequency ceiling; sets `FAST_RAPL` PLR bit in `tpmi.plr_mailbox_interface`; updates `LEAF_PERF_STATUS` via HPM 0x16

**Fast RAPL exit flow:**
1. Instantaneous power drops below Fast RAPL threshold
2. Next 500 µs timer: FastRAPL PID relaxes ceiling; `RAPL_PERF_LIMIT` recalculated (dominated by PL1/PL2 ceilings)
3. HPM 0x14 sent without `FAST_RAPL` being limiting contributor
4. CBB PCode removes Fast RAPL ceiling; `FAST_RAPL` PLR bit clears; `pem_excursion_bit` cleared by SW (RW0C)

**Response time (PLR):** < 4 ms from power excursion to frequency ceiling applied

---

## Section 6: Corner Cases & Error Handling

| Scenario | Expected Behavior |
|----------|-----------------|
| Fast RAPL fires simultaneously with Socket PL2 throttle | min(Fast RAPL, PL2) is enforced; both PLR bits set in `RAPL_PERF_LIMIT` |
| Fast RAPL threshold set below TDP | Spurious throttling expected; `pem_excursion_bit` frequently set; validate with intentional under-threshold config |
| Fast RAPL while PMax is also limiting | min(PMax, Fast RAPL) enforced — most restrictive wins |
| Short transient clears before next 500 µs poll | Fast RAPL may not fire; PEM bit not set; test must ensure transient is sustained across ≥1 poll cycle |
| `pem_excursion_bit` clear behavior | **RW0C** — write 0 to clear; read does not auto-clear; avoid read-race in status checks |
| SVID disabled / invalid IMON telemetry | Fast RAPL cannot evaluate power → drives `0xFF` (max/no-effective-limit); test must verify SVID health pre-test |
| Stale CLOS limits: 500 µs vs 1 ms interaction | If CLOS limits updated at 1 ms cadence, Fast RAPL HPM 0x14 at 500 µs may use stale CLOS values; PrimeCode must rerun calculation before sending |
| CBB count mismatch (DMR=4 vs NWP=2) | Test loops must iterate over cbb0/cbb1 only; CBB2/CBB3 register access will fail |
| BIOS power limit lock enabled | Fast RAPL testing blocked; `TurboPowerLimitLock` must be disabled pre-test |

---

## Section 7: Security / Safety / Policy

- Fast RAPL is a firmware-level power protection mechanism; threshold configuration is privilege-controlled
- Test stimulus (PMX cpu_rapl) requires OS ring-0 and PMX framework access
- Intentional power overload to trigger Fast RAPL must be done in controlled silicon lab environment
- PEM/PLR register reads require PythonSv / namednodes privileged access

---

## Section 8: References

- [DMR RAPL Simplification FAS — Fast RAPL](https://docs.intel.com/documents/pm_doc/src/server/DMR/PM%20Features/DMR_RAPL_Simplification.html#fast-rapl) — Fast RAPL architecture; 500 µs loop; no EWMA; non-programmable threshold; PLR < 4 ms
- [Primecode Fast RAPL Flow](https://docs.intel.com/documents/primecode/primecode_two/firmware_architecture/flows_pm_features/fast_rapl.html) -- FastRAPL PID description; lightweight loop; target power vs. TDP; `0xFF` fallback on invalid telemetry
- [DMR Primecode RAPL DMR Driver](https://docs.intel.com/documents/primecode/primecode_two/firmware_architecture/ip_drivers_and_libraries/rapl_dmr.html) -- 500 µs timer handler; folded HPM; CLOS limit interaction; stale-value concern
- [HPM Message Specification](https://docs.intel.com/documents/primecode/has/dmr/hierarchical_pm/hpm_message_specification.html) -- `RAPL_PERF_LIMIT` (opcode 0x14) `FAST_RAPL` bit; `LEAF_PERF_STATUS` (HPM 0x16)
- [GNR HPM Fast RAPL](https://docs.intel.com/documents/pm_doc/src/server/GNR/Features/hpm_fast_rapl/hpm_fast_rapl.html) -- Historical Fast RAPL HPM → unified `RAPL_PERF_LIMIT`; reason-bit addition
- [Wave3 PEM HAS](https://docs.intel.com/documents/pm_doc/src/server/Wave3_common/PEM/PEM_HAS.html) -- `PEM_STATUS[FAST_RAPL]` bit 9; RW0C semantics
- [NWP PM MAS §3](https://docs.intel.com/documents/custom-xeon/newport-docs/mas/pm/nwp_imh_soc_pm_mas.html) -- ZBB list (cites FastRAPL as ZBB); confirm against latest revision
